version: 0.2
   
phases:
    install:
        runtime-versions:
            nodejs: 10
        commands:
            - echo Installing source NPM dependencies...
            - sudo npm install -g serverless
            - npm install

    build:
        commands:
            - npm run deploy
